Classify this capture. Return ONLY valid JSON, no other text, no markdown.

Content: {content}
Source: {source}
Type: {capture_type}
Captured at: {captured_at} IST
{type_context}
{quote_hint}

Date and event rules:
- date_phrase: copy the exact words from the text that name a date or time ("next Friday", "tomorrow at 3pm", "on the 25th", "September 25"). Do not compute or rewrite. Return null if no date or time is mentioned.
- is_event: true if something is happening *at* a time — a call, meeting, appointment, or any scheduled occurrence. False if the date is a deadline the task must be done *by*, or if no date is mentioned.

Return exactly this structure:
{{
  "category": "learning|thoughts|work|life|resources|health",
  "subcategory": "wayclear|accrediq|finance|quotes|grocery|tech|hacks|music|null",
  "tags": ["tag1", "tag2", "tag3"],
  "summary": "under 50 words",
  "action_class": "record|task|agent|alert|build",
  "date_phrase": null,
  "is_event": false
}}

Context: User is Ashish, solo founder building WayClear (road safety) and AccredIQ (IRC accreditation).
SMS debits = life/finance. WhatsApp = life/people. URLs = resources. Tasks/todos = work.
